import './styles.css'

function CenteredCards({children}) {

    return (
        <div>
            <span>
                {children}
            </span>
        </div>
    )
}

export default CenteredCards